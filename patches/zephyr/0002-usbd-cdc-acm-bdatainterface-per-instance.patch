diff --git a/subsys/usb/device_next/class/usbd_cdc_acm.c b/subsys/usb/device_next/class/usbd_cdc_acm.c
index cff61406d68..80ca6a6932d 100644
--- a/subsys/usb/device_next/class/usbd_cdc_acm.c
+++ b/subsys/usb/device_next/class/usbd_cdc_acm.c
@@ -559,6 +565,11 @@ static int usbd_cdc_acm_init(struct usbd_class_data *const c_data)
 
 	desc->if0_union.bControlInterface = desc->if0.bInterfaceNumber;
 	desc->if0_union.bSubordinateInterface0 = desc->if1.bInterfaceNumber;
+	/* bDataInterface is instance-specific: the static descriptor defaults
+	 * to 1, which is wrong for any CDC ACM instance beyond the first
+	 * (for example the DataCollect data port).
+	 */
+	desc->if0_cm.bDataInterface = desc->if1.bInterfaceNumber;
 
 	if (cfg->if_desc_data != NULL && desc->if0.iInterface == 0) {
 		if (usbd_add_descriptor(uds_ctx, cfg->if_desc_data)) {
