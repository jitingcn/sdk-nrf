diff --git a/drivers/led_strip/ws2812_spi.c b/drivers/led_strip/ws2812_spi.c
index 7f6fa5f3a36..125b276090d 100644
--- a/drivers/led_strip/ws2812_spi.c
+++ b/drivers/led_strip/ws2812_spi.c
@@ -139,6 +139,11 @@ static int ws2812_strip_update_rgb(const struct device *dev,
 	size_t i;
 	int rc;
 
+	/* The buffer is a static array sized for the strip length. */
+	if (num_pixels > cfg->length) {
+		return -EINVAL;
+	}
+
 	/*
 	 * Convert pixel data into an SPI bitstream. The bitstream contains
 	 * pixel data in color mapping on-wire format (e.g. GRB, GRBW, RGB,
